using UnityEngine;
using System.Collections;

public enum DamageType
{
    Explode,
    Energy,
    Impact,
    Fragment
}

public enum ArmorType
{
    Field, // Used for shield.
    Atom, // Used for shield.
    Nuclear, // High tech (Not so high) armor material.
    Steel, // Reinforced ship struct that used for ship armor.
    Struct // Standard ship struct made with iron and so on.
}

